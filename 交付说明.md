# 交付说明

## 运行环境
- C++ 出清引擎：g++（支持 C++17）或任意 C++17 编译器；Windows 可直接运行已编译的 spot_platform.exe
- 网页端：Node.js 18+（仅运行 server.js 需要；网页本身无框架依赖）

## 启动步骤
1. 编译引擎：`cd spot_platform && make`（或 `g++ -std=c++17 -O2 -o spot_platform main.cpp market.cpp`）
2. 命令行使用：`./spot_platform --help` 查看全部选项
3. 网页端：把编译出的 spot_platform.exe 放到 spot_platform_web/ 目录，
   然后 `cd spot_platform_web && npm run dev`，浏览器打开提示的地址

## 常见问题
- 网页提示"无法连接后端计算服务"：说明 server.js 未启动，或引擎 exe 未放到网页目录
- 控制台中文乱码：Windows 下先执行 `chcp 65001`
- 申报校验未通过：检查报价单调性（发电侧递增、用户侧递减）、段数 ≤ 10、数量 > 0
